name: ald_machine_app
description: A Flutter app for ALD machine control

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  shared_preferences: ^2.0.15
  json_annotation: ^4.4.0
  intl: ^0.17.0
  uuid: ^3.0.4
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.1
  fl_chart: ^0.55.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7
  json_serializable: ^6.1.4

flutter:
  uses-material-design: true
  assets:
    - assets/ald_system_diagram.png

