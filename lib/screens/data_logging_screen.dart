import 'package:flutter/material.dart';

class DataLoggingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Data Logging Screen'),
    );
  }
}